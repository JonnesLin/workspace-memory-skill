# Gemini CLI Entry

The canonical workspace agent instructions for this repo live in `AGENTS.md`.

Read `AGENTS.md` for session start protocol, read/write rules, and memory-file responsibilities. For non-trivial tasks also consult `PROJECT_PROGRESS.md` and `WORKING_MEMORY.md` before project-specific reasoning or edits.

Do not duplicate rules here. Add Gemini-specific overrides below only when they genuinely differ from the canonical protocol.

## Gemini Specific Overrides

_(none)_
